name: Schedule – Linux

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *'
  push:
    branches: [ 'none' ]

jobs:
  call:
    uses: ./.github/workflows/run-tests.yml
    with:
      runner-label: '["self-hosted", "Linux"]'
