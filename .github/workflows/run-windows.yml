name: Run payments on Windows

on:
  schedule:
    - cron: "5 7 * * *" # Daily @8.05
  workflow_dispatch:

jobs:
  windows:
    if: github.repository == 'grzegorz-ozanski/payments'
    uses: ./.github/workflows/run-payments.yml
    permissions:
      contents: write
    with:
      runner_label: '["self-hosted", "Windows"]'
      event_name: ${{ github.event_name }}
      browser_profile_dir: 'C:\actions-runner\_work\payments'
    secrets:
      GMAIL_USERNAME: ${{ secrets.GMAIL_USERNAME }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      GMAIL_EMAIL_ADDRESS: ${{ secrets.GMAIL_EMAIL_ADDRESS }}
      BROWSER_REPO_TOKEN: ${{ secrets.BROWSER_REPO_TOKEN }}
      PAYMENTS_TESTDATA_REPO_TOKEN: ${{ secrets.TESTDATA_REPO_TOKEN }}
      CREDENTIALS: ${{ secrets.CREDENTIALS }}
      REPORTS_TS_HOST_IP: ${{ secrets.REPORTS_TS_HOST_IP }}
      REPORTS_TS_PORT: ${{ secrets.REPORTS_TS_PORT }}
      REPORTS_USER: ${{ secrets.REPORTS_USER }}
      REPORTS_SSH_KEY: ${{ secrets.REPORTS_SSH_KEY }}
